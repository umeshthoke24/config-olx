spring:
  data:
    mongodb:
      database: BlackListed
      host: localhost
      port: 27017
  datasource:
    driver-class-name: com.mysql.cj.jdbc.Driver
    password: root
    url: jdbc:mysql://localhost:3306/olxmasterdata
    username: root
  jpa:
    hibernate:
      ddl-auto: update
